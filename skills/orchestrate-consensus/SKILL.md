---
name: orchestrate-consensus
description: Follow the installed Orchestrate Consensus guide for three finalized Discovery results.
disable-model-invocation: true
---

Use this skill only when explicitly invoked. Resolve the installed `orchestrate` executable, run `orchestrate guide consensus`, and follow that guide exactly. Stop at the Consensus boundary.
