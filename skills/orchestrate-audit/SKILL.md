---
name: orchestrate-audit
description: Follow the installed Orchestrate Audit guide for an adopted Agreement and external implementation.
disable-model-invocation: true
---

Use this skill only when explicitly invoked. Resolve the installed `orchestrate` executable, run `orchestrate guide audit`, and follow that guide exactly. Stop at the Audit boundary.
