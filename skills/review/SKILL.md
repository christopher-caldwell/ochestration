---
name: review
description: Independently review the current Orchestrate Build delivery phase.
disable-model-invocation: true
---

# Review

Read `action.json` and inspect only its exact submitted commit. Run relevant checks, write a
concrete report, and write the receipt as `pass`, `changes_required`, or `blocked`. Do not repair
product source, change the adopted scope, or edit controller state.

