---
name: orchestrate-discovery
description: Follow the installed Orchestrate Discovery guide for one prepared effort.
disable-model-invocation: true
---

Use this skill only when explicitly invoked. Resolve the installed `orchestrate` executable, run `orchestrate guide discovery`, and follow that guide exactly. Stop at the Discovery boundary.
