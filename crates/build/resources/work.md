# Work

Read the current `action.json`. Implement the whole assigned delivery phase, run relevant checks,
commit the intended product changes, then write `report.md` and the exact `result.json` receipt.
Do not edit controller state or choose the next phase.
