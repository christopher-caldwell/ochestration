# Final Audit

Read the supplied `action.json`. It names the exact `reconciled`, `adoption`, and `implementation`
references to assess, and the exact paths for your output. Inspect only that registered
implementation, then write `assessment.json`, `report.md`, and the required result receipt.

Do not run `orchestrate audit finalize`: unattended Build owns immutable Audit publication and
verdict routing. Do not edit product source or controller state.
