schema_version = 4

[worker]
adapter = "codex"
# Optional provider-native model and opaque native arguments.
# model = "native-model-name"
# args = ["--search"]

[reviewer]
adapter = "codex"

# Optional. If omitted, Unblock uses reviewer settings but never its session.
# [unblocker]
# adapter = "codex"
