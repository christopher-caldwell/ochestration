# Ticket workflow

Prepare supplemental notes with `$prep-discovery-ticket`, paste the original ticket verbatim, and
review the prepared file. Invoke `$discovery` from several independent model windows with the same
path. Give two or more finalized outputs to `$reconcile`, approve the precise result only if its
binding requirements are right, build externally, register the commit, and invoke `$audit`.
