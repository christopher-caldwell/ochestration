Follow `role-guide.md` for this bounded Work turn. Read `action.json`, the `binding_requirements` path it names, the exact `reconciled_discovery` path, and `detailed_plan.md`. Also read any current evidence file named by action.json. Use only the shell for local reads/writes and Git; do not invoke UI, MCP, browser, external services, or Orchestrate commands. Make no network request, do not read or invent credential values, and do not edit the authority or controller-owned files. Work only in this disposable synthetic repository and satisfy the assigned phase if the supplied current evidence permits it. Write the assigned product note, `report.md`, and `result.json` as the guide directs; attempt only the intended scoped Git commit when outcome is complete. Make no claims about the real KV Core service. Treat labels R-010–R-049 in the packet as the KV Core source-contract namespace, not as Orchestrate’s frozen Build RIDs.
