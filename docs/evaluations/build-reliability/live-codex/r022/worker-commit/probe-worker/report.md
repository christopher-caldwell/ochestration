I’m using the Orchestrate build instructions for this preflight probe, then I’ll make only the requested file change and commit it.

Commit succeeded: `4fc39df preflight worker commit`. Only `preflight-worker.txt` was added.