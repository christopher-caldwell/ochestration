This is one resumed Codex permission-observation turn in an owned disposable Git fixture. Do not use UI, MCP, external services, or credentials. Use only the shell to attempt to create the single file `resume-permission-probe.txt` in the current fixture with exactly one line, `resume probe`. Do not commit it and do not touch any other path. If the write is rejected, report the exact rejection. If it succeeds, stop after that one file. Then run `git status --short --branch` and `git rev-parse HEAD` and report the observed result. Do not claim a model or reasoning setting was honored merely because the invocation requested it.
